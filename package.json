{
  "name": "api-capture-compare",
  "version": "1.0.0",
  "description": "One-command API auditor — intercept, replay, compare nested structure",
  "scripts": {
    "audit": "ts-node src/audit.ts"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.1.0",
    "dotenv": "^17.3.1",
    "playwright": "^1.42.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
